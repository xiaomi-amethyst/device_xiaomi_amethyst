<?xml version="1.0" encoding="utf-8"?>
<!--
     Copyright (C) 2015-2016 The CyanogenMod Project
                   2017-2018 The LineageOS Project

     Licensed under the Apache License, Version 2.0 (the "License");
     you may not use this file except in compliance with the License.
     You may obtain a copy of the License at

          http://www.apache.org/licenses/LICENSE-2.0

     Unless required by applicable law or agreed to in writing, software
     distributed under the License is distributed on an "AS IS" BASIS,
     WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
     See the License for the specific language governing permissions and
     limitations under the License.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="org.lineageos.settings"
    android:versionCode="1"
    android:versionName="1.0"
    android:sharedUserId="android.uid.system">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.GET_TASKS" />
    <uses-permission android:name="android.permission.ACCESS_SURFACE_FLINGER" />
    <uses-permission android:name="android.permission.INTERACT_ACROSS_USERS_FULL" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />

    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" />

    <protected-broadcast android:name="com.android.systemui.doze.pulse" />

    <uses-sdk
        android:minSdkVersion="24"
        android:targetSdkVersion="30" />

    <application
        android:label="@string/app_title"
        android:persistent="true"
        android:defaultToDeviceProtectedStorage="true"
        android:directBootAware="true"
        android:theme="@style/Theme.SubSettingsBase.Expressive">

        <receiver
            android:name=".BootCompletedReceiver"
            android:exported="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            tools:replace="android:authorities" />

        <receiver
            android:name=".Startup"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".ApplyOnBootReceiver"
            android:exported="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

        <!-- Amethyst Parts -->
        <activity
            android:name=".amethystparts.AmethystPartsActivity"
            android:label="@string/amethyst_parts_title"
            android:theme="@android:style/Theme.DeviceDefault.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="com.android.settings.action.IA_SETTINGS" />
            </intent-filter>
            <meta-data
                android:name="com.android.settings.category"
                android:value="com.android.settings.category.ia.system" />
            <meta-data
                android:name="com.android.settings.summary"
                android:resource="@string/amethyst_parts_summary" />
            <meta-data
        	    android:name="com.android.settings.icon"
        	    android:resource="@drawable/ic_amethyst" />
        	<meta-data
                android:name="com.android.settings.order"
                android:value="150" />
        </activity>

        <!-- Thermal Settings -->
        <activity
            android:name=".thermal.ThermalComposeActivity"
            android:label="@string/thermal_title"
            android:theme="@android:style/Theme.DeviceDefault.NoActionBar"
            android:exported="true" />
            
        <service
            android:name=".thermal.ThermalService"
            android:exported="false" />
            
        <!-- Bypass Charging -->
        <activity
            android:name=".charge.ChargeActivity"
            android:label="@string/charge_bypass_title"
            android:theme="@android:style/Theme.DeviceDefault.NoActionBar"
            android:exported="false" />

        <service
            android:name=".charge.ChargeTileService"
            android:label="@string/charge_bypass_title"
            android:icon="@drawable/ic_bypass_charge_enabled"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Charge Limit -->
        <activity
            android:name=".chargelimit.ChargeLimitActivity"
            android:label="@string/charge_limit_title"
            android:theme="@android:style/Theme.DeviceDefault.NoActionBar"
            android:exported="false" />

        <service
            android:name=".chargelimit.ChargeLimitService"
            android:exported="false" />

        <!-- HyperCharge Settings, Service and Tile -->
        <activity
            android:name=".hypercharge.HyperChargeSettingsActivity"
            android:label="@string/hypercharge_title"
            android:theme="@android:style/Theme.DeviceDefault.NoActionBar"
            android:exported="false" />

        <service
            android:name=".hypercharge.HyperChargeService"
            android:exported="false" />

        <service
            android:name=".hypercharge.HyperChargeTileService"
            android:label="@string/hypercharge_tile_label"
            android:icon="@drawable/ic_qs_hypercharge_on"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Tile Handler helper activity -->
        <activity
            android:name=".TileHandlerActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE_PREFERENCES" />
            </intent-filter>
        </activity>

        <!-- Core Control -->
        <activity
            android:name=".corecontrol.CoreControlActivity"
            android:label="@string/core_control_title"
            android:exported="false" />

        <!-- Clear Speaker -->
        <activity
            android:name=".speaker.ClearSpeakerActivity"
            android:label="@string/clear_speaker_title"
            android:theme="@android:style/Theme.DeviceDefault.NoActionBar"
            android:exported="false" />
        
        <!-- CPU Control -->
        <activity
            android:name=".kernelmanager.KernelManagerActivity"
            android:label="@string/kernel_manager_title"
            android:exported="false" />

        <!-- GPU Control -->
        <activity
            android:name=".gpumanager.GpuManagerActivity"
            android:label="@string/gpu_manager_title"
            android:theme="@android:style/Theme.DeviceDefault.NoActionBar"
            android:exported="false" />

        <!-- Refresh Rate Settings -->
        <activity
            android:name=".refreshrate.RefreshSettingsActivity"
            android:label="@string/refresh_title"
            android:exported="false" />

        <service
            android:name=".refreshrate.RefreshService"
            android:exported="false" />
        
        <!-- KProfiles activity -->
        <activity
            android:name=".kprofiles.KprofilesSettingsActivity"
            android:exported="true"
            android:label="@string/kprofiles_title" />
            
        <!-- KProfiles tile service -->
        <service
            android:name=".kprofiles.KProfilesModesTileService"
            android:exported="true"
            android:icon="@drawable/ic_kprofiles"
            android:label="@string/kprofiles_title"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE"/>
            </intent-filter>
            <meta-data android:name="android.service.quicksettings.TOGGLEABLE_TILE"
                android:value="true" />
        </service>

        <!-- Display Saturation activity -->
        <activity
            android:name=".saturation.SaturationActivity"
            android:label="@string/saturation_title"
            android:exported="false" />

        <!-- Display Saturation tile service -->
        <service
            android:name=".saturation.SaturationTileService"
            android:label="@string/saturation_title"
            android:icon="@drawable/ic_saturation_tile"
            android:exported="true"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

    </application>
</manifest>
